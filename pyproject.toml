[project]
name = "log-analyzer"
version = "0.1.0"
description = "connection logs analyzer in Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "pytest>=9.1.1",
]
[tool.pytest.ini_options]
pythonpath = ["."]